# Hyperparameters for neural networks

Word_Encoder = {"num_letters": 3,
                "embedding_dim": 100,
                "hidden_size": 200,
                "dropout_rate": 0.2
                }

Span_Encoder = {"input_size": 10,
               "hidden_size": 100,
               "dropout_rate": 0.2
                }

Parser = {"input_size": 10,
          "hidden_size": 200,
          "dropout_rate": 0.3
          }

